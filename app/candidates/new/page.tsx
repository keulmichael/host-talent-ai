"use client";import{useRouter}from"next/navigation";import{FormEvent}from"react";export default function P(){const r=useRouter();async function s(e:FormEvent<HTMLFormElement>){e.preventDefault();const x=await fetch("/api/candidates",{method:"POST",body:new FormData(e.currentTarget)});const d=await x.json();if(x.ok)r.push("/candidates");else alert(d.error)}return <div className="card"><h1>Importer un candidat</h1><form onSubmit={s}><div className="field"><label>Nom<input name="fullName" required/></label></div><div className="field"><label>Localisation<input name="location"/></label></div><div className="field"><label>CV<input type="file" name="file" accept=".pdf,.docx,.txt"/></label></div><div className="field"><label>Ou texte du CV<textarea name="rawText"/></label></div><button className="btn">Importer</button></form></div>}