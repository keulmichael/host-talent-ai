"use client";import{useRouter}from"next/navigation";import{FormEvent}from"react";export default function P(){const r=useRouter();async function s(e:FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const x=await fetch("/api/jobs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)});const d=await x.json();if(x.ok)r.push(`/jobs/${d.id}`);else alert(d.error)}return <div className="card"><div className="eyebrow">MISSION</div><h1>Créer une mission</h1><form onSubmit={s}><div className="grid"><div className="field"><label>Intitulé<input name="title" required/></label></div><div className="field"><label>Client<input name="clientName"/></label></div><div className="field"><label>Localisation<input name="location"/></label></div></div><div className="field"><label>Description<textarea name="description" required/></label></div><div className="field"><label>Indispensables<textarea name="mustHave"/></label></div><div className="field"><label>Souhaitables<textarea name="shouldHave"/></label></div><div className="field"><label>Optionnels<textarea name="optional"/></label></div><button className="btn">Créer</button></form></div>}